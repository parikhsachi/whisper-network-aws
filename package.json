{
  "name": "whisper-network-aws",
  "private": true,
  "workspaces": ["dashboard", "encoder"],
  "scripts": {
    "dev": "npm -w dashboard run dev",
    "build": "npm -w dashboard run build",
    "encode": "npm -w encoder run encode"
  }
}


import { createPaginator } from "@smithy/core";
import { CleanRoomsClient } from "../CleanRoomsClient";
import { ListConfiguredTablesCommand, } from "../commands/ListConfiguredTablesCommand";
export const paginateListConfiguredTables = createPaginator(CleanRoomsClient, ListConfiguredTablesCommand, "nextToken", "nextToken", "maxResults");
